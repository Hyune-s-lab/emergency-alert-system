spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      default_batch_fetch_size: 1000
      open-in-view: false
      show-sql: true

  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:8.0.30:////emergency-alert-system
